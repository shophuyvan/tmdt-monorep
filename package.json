{
  "name": "tmdt-monorepo",
  "private": true,
  "workspaces": [
    "mini",
    "admin",
    "api"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "fmt": "prettier -w . || true",
    "lint": "echo \"lint placeholder\"",
    "postinstall": "cd api && npx prisma generate"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}